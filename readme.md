pre requ:  standard Python 3 


to run the code you can use:
python bank.py  > run.log

where all the output will go into run.log to not have to deal with some of the data lost due to buffer size in the terminal


or you can do 
python bank.py 

to have the output into the console
